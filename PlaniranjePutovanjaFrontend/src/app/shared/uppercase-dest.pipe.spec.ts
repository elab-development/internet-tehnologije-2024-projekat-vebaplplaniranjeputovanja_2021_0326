import { UppercaseDestPipe } from './uppercase-dest.pipe';

describe('UppercaseDestPipe', () => {
  it('create an instance', () => {
    const pipe = new UppercaseDestPipe();
    expect(pipe).toBeTruthy();
  });
});
